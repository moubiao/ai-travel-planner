# 🎬 演示脚本（Demo Script）

> 面向竞赛答辩/作品展示的 5 分钟演示流程。前提：后端 `uvicorn app.main:app --port 8003`、前端 `npm run dev` 均已启动，浏览器打开 http://localhost:5173。

---

## 第 1 幕：产品介绍（30 秒）

**开场白**：
> "这是一款基于 LLM Agent 架构的智能旅行规划助手。用户只需要说出旅行需求，系统会像一支专业旅行顾问团队一样协作，生成个性化行程方案。"

**操作**：展示首页——自然语言输入框、🎤 语音按钮、表单、示例标签。

---

## 第 2 幕：多Agent协作生成方案（90 秒）

**操作**：
1. 点击示例标签「成都3天2夜，两个人，预算4000，喜欢美食和历史文化，不要购物」
2. 点击「生成旅行方案」，等待约 15 秒

**讲解**（生成期间）：
> "生成过程中，系统内部运行着一个 LangGraph 编排的多 Agent 工作流。可以看到，它依次完成了需求分析、RAG 知识库检索、天气查询，然后四位专家子 Agent 并行协作——景点 Agent 负责筛选景点，美食 Agent 挑选餐厅，路线 Agent 按地理位置优化行程，预算 Agent 分配费用——最后由主 Agent 综合所有建议生成方案。"

**操作**：方案生成后，滚动展示：
1. **🤖 Agent 规划过程**卡片（10 个步骤时间线，含质量评估）——点击各步骤展示详情
2. **🌤️ 天气预报**卡片——若演示模式有雨天，指出"雨天自动调整"能力
3. **🗓️ 每日行程**——指出每个推荐都带理由（可解释性）
4. **📚 知识库引用来源**——"所有推荐都来自我们自建的城市知识库，可追溯、不胡编"

---

## 第 3 幕：对话式调整（30 秒）

**操作**：在底部调整框输入「预算减少500元，第三天换成室内行程」，点击调整。

**讲解**：
> "方案生成后还可以继续对话式调整，AI 会基于当前方案联动修改，而不是重新生成。"

**操作**：展示调整后的方案（预算、行程变化）。

---

## 第 4 幕：多模态图片理解（60 秒）

**操作**：
1. 滚动到「📷 上传照片，AI 帮你找相似景点」
2. 上传一张旅行照片（如景点照片）
3. 等待分析（首次约 10 秒，后续 1-2 秒），展示推荐结果

**讲解**：
> "系统还支持多模态理解。我们本地部署了 Chinese-CLIP 图文模型，上传照片后，AI 提取图片的语义特征，与知识库中所有景点做相似度匹配，推荐风格相近的目的地。全程本地推理，不依赖外部 API。"

---

## 第 5 幕：用户系统与历史记录（30 秒）

**操作**：
1. 点击「💾 保存方案」→ 输入标题 → 保存
2. 点击顶部「历史记录」→ 展示方案列表 → 点击查看

**讲解**：
> "登录用户可以把自己的方案保存到历史记录，跨会话回看。"

---

## 第 6 幕：行程校验 + 收尾（30 秒）

**操作**：滚动到「⚠️ 行程校验提示」（若有），指出 AI 自动检测的问题（如雨天户外、跨区奔波）。

**结尾**：
> "总结一下，这个系统集成了大模型应用、RAG 知识增强、Agent 任务规划、外部工具调用、多模态理解五大能力，是一个完整的 LLM Agent 应用。谢谢！"

---

## 备选演示（无网络依赖）

- **雨天调整演示**：修改 `backend/.env` 中 `WEATHER_DEMO_MODE=rain`，重启后端，生成方案可展示"雨天自动替换室内景点"的完整决策链
- **语音输入演示**：Chrome 中点击 🎤 说"昆明三天两夜，一个人，预算两千"
