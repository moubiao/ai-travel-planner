# 前端环境变量配置示例
# 使用方法：复制本文件为 .env（放在 frontend 目录下），然后填入你的配置

# 高德地图 JS API key（用于结果页「每日行程地图」的真实地图渲染）
# 获取方式：https://console.amap.com → 应用管理 → 你的应用 → 添加 Key → 服务平台选「Web端(JS API)」
# 注意：与后端 .env 的 AMAP_API_KEY（Web服务类型）不同，需要另外创建
# 未配置时地图自动降级为 SVG 示意地图（不影响演示）
VITE_AMAP_JS_KEY=
