# 🏆 答辩要点（Pitch Points）

> 面向竞赛评委的核心亮点与问答准备。

---

## 一、项目一句话介绍

**基于 LLM Agent 架构的智能旅行规划系统**：多 Agent 协作 + RAG 知识增强 + 外部工具调用 + 多模态理解，实现多约束条件下的个性化旅行方案自动生成。

---

## 二、五大核心亮点

### 亮点 1：多 Agent 协作架构（LangGraph）
- **问题**：单体 Prompt 生成方案质量不稳定、职责不清、无法并行
- **方案**：LangGraph 编排 10 节点工作流，4 位专家子 Agent（景点/美食/路线/预算）**并行**协作，主 Agent 汇总，末尾质量评估节点把关
- **价值**：各子 Agent 职责单一、Prompt 精简、可并行加速；方案每个推荐都带"子 Agent 推荐理由"（**可解释性**）

### 亮点 2：RAG 知识增强 + 可追溯引用
- **问题**：LLM 幻觉（编造景点/价格/开放时间）
- **方案**：自建成都+昆明两城知识库（60 景点/40 美食/30 酒店，手工整理无版权风险），BGE 中文向量 + FAISS 检索 + Rerank 精排，方案强制引用知识库 `source_id`，前端展示引用来源
- **价值**：方案 100% 可追溯、价格信息真实可靠；演示"成都室内亲子景点"等检索场景

### 亮点 3：多模态 + 工具调用 + 天气感知
- **多模态**：本地部署 Chinese-CLIP，上传照片 → 语义匹配推荐相似景点（全本地推理）
- **工具调用**：和风天气（真实预报，无 key 时演示数据降级）、高德距离 API（无 key 时坐标计算兜底）
- **天气感知**：雨天自动调整——检索室内景点、路线 Agent 输出替换建议、行程校验兜底检测

### 亮点 4：方案质量自动评估闭环（生成 → 评估 → 迭代）
- **问题**：LLM 生成结果质量不可控，无法量化
- **方案**：规则 + LLM 双引擎 5 维评分（预算符合度/路线合理性/引用真实率/结构完整性/需求贴合度），总分 <70 自动携带改进建议重新生成（LangGraph 条件路由回退），前端展示评分卡
- **价值**：把"生成结果不可验证"变为"可量化、可改进、可追溯"——这是区别于普通 Demo 的工程化分水岭

### 亮点 5：旅行意图理解（隐含意图推断）
- **问题**：用户表达的信息往往不完整（"带5岁小孩"背后是安全与节奏约束）
- **方案**：需求解析阶段输出意图层（核心诉求/关键约束/隐含需求），注入子 Agent 与主 Agent 的生成过程，前端展示"意图理解卡片"
- **价值**：方案真正"懂"用户——实测"一家三口带5岁小孩，节奏慢一点"被解析出"亲子安全/节奏慢/预算适中"约束，方案自动呈现慢节奏、清淡饮食、亲子友好酒店；该能力与隐含意图识别研究方向联动，可迁移到论文场景

---

## 三、技术栈与架构

| 层 | 技术 |
|---|---|
| 后端 | Python 3.13 + FastAPI |
| 前端 | Vue3 + Vite + Element Plus + ECharts |
| Agent | LangGraph（工作流编排 + 并行子图） |
| LLM | DeepSeek API（JSON 模式 + 思考模式控制） |
| RAG | BGE-small-zh + FAISS + bge-reranker（全部本地 GPU 推理） |
| 多模态 | Chinese-CLIP（本地） |
| 工具 | 和风天气 / 高德地图（可配置降级） |
| 数据 | SQLite（用户 + 历史方案），JWT 认证 |

---

## 四、可能被问到的问题（Q&A 准备）

**Q1：为什么选 LangGraph 而不是 LangChain Agent？**
> LangGraph 提供显式的图结构工作流，节点/边/状态可控，适合我们这种"规划类"多阶段任务（而非自由对话式工具调用）；支持并行子图（4 位专家 Agent 并发执行），执行过程可记录 trace 用于展示与调试。

**Q2：RAG 检索质量如何保证？**
> 三保险：① BGE 中文向量语义召回；② 元数据过滤（城市/类型/室内外/预算）；③ bge-reranker 交叉编码精排。检索质量有专项测试（5 类典型问题验证 top-k 相关性，如"昆明冬季观鸥"命中海埂大坝/翠湖/大观楼）。

**Q3：方案里有没有 LLM 幻觉？**
> 方案生成 Prompt 强制要求从知识库注入的候选中选择并标注 source_id，后端还会做引用校验（所有 source_id 必须能映射到知识库），无法映射的引用会被剔除。

**Q4：天气/地图是真实数据吗？**
> 架构上对接了和风天气与高德真实 API；未配置 key 时自动降级为演示数据与坐标计算，保证系统任何环境可演示。配置 key 后即为真实预报与真实驾车距离。

**Q5：多模态模型为什么本地部署？**
> Chinese-CLIP 对中文场景理解好、权重公开、单卡推理延迟 <1s，避免外部 API 依赖与数据出域问题。模型经 ModelScope（魔搭）下载，国内网络友好。

**Q6：扩展性？**
> 知识库是 JSON 结构化，新增城市只需加一个数据目录 + 重建索引（`python build_index.py`）；新增子 Agent 只需在 specialists 节点注册一个函数 + 一个 Prompt 模板。

**Q7：方案质量怎么保证？**
> 双保险：① 质量评估节点（规则 4 维 + LLM 1 维）量化打分，总分 <70 时 LangGraph 条件路由自动携带改进建议回到生成节点重试一次；② 行程校验节点（跨区奔波/雨天户外/预算偏差/门票占比）兜底检测。实测方案稳定在 85-96 分（优秀）。

**Q8：意图理解模块的技术含量在哪？**
> 不是简单的关键词抽取，而是让 LLM 在结构化解析时同步输出"隐含意图层"（约束识别 + 未言明需求推断），并作为上下文注入后续全部生成节点——方案生成从"满足显式需求"升级为"理解真实意图"。这也是我研究方向的工程验证场景。

---

## 五、工程化亮点（加分项）

- 异常代理环境处理（应用入口清理，DeepSeek 稳定调用）
- DeepSeek 思考模式控制（`extra_body` 关闭推理，解决输出为空问题）
- 统一的错误处理与日志、启动自动初始化数据库
- JWT 认证 + PBKDF2 密码哈希 + 跨用户数据隔离
- 5 个自动化验收测试脚本（RAG 质量 / Agent 工作流 / 认证 / 多模态 / 端到端）+ 质量评估专项测试
- 完整测试驱动开发过程：每个阶段均有 TestClient 断言 + 浏览器端到端验证
